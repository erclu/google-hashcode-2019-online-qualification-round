[pytest]
addopts =
    --tb=short
    --basetemp=.pytest_tmpdir
    --instafail
    --spec
    --cov=.
    --cov-report=html:.coverage_report
    --no-cov-on-fail
filterwarnings =
    # monkeypatch bug in pytest-spec
    ignore::UserWarning
